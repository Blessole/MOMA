<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bookmarkns">
	<!-- 회원이 좋아요 했는지 체크 -->
	<select id="select" resultType="bookmark">
		select * from likes where mno = #{mno} and cno = #{cno}
	</select>
	
	<insert id="insert" parameterType="hashmap">
		<selectKey keyProperty="lno" order="BEFORE" resultType="integer">
				select nvl(max(lno), 0) + 1 lno from likes
		</selectKey>
		insert into likes values(#{lno}, #{mno}, #{cno})
	</insert>
	
	<delete id="delete" parameterType="hashmap">
		delete likes where mno = #{mno} and cno = #{cno}
	</delete>
	
	<!-- 마이 페이지 - 좋아요 체크 수 -->
	<select id="getTotalMy" parameterType="integer" resultType="integer">
		select count(*) from likes where mno = #{mno}
	</select>
	
	<!-- 마이 페이지 - 메인 -->
	<select id="myMainMenu" parameterType="integer" resultType="likes">
		select * from (select rowNum rn, e.* from (
		select c.*, cname, spot
		from likes l, content c where l.cno = c.cno and l.mno = #{mno}
		and c.del='n' order by lno desc) e)
		where rn between 1 and 4
	</select>
	
	<!-- 마이 페이지 - 좋아요 리스트 -->
	<select id="myList" parameterType="hashmap" resultType="likes">
		select * from (select rowNum rn, e.* from (
		select c.*, poster, cname, clevel, genre, hours, start_date, director, actor
		from likes b, content c where l.cno = c.cno and l.mno = #{mno}
		and c.del='n' order by lno desc) e)
		where rn between #{startRow} and #{endRow}
	</select>
</mapper>